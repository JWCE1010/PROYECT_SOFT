<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Proyecto</title>
</head>
<body>
    <div id="div1">
  <img class="card-img-top mx-auto"  src="/img/producto.jpeg" alt="" width="900px" height="1000px">
<div class="container p-4" id="div2">
    <div class="row">
        {{#each producto}}
        <div class="col-md-3">
            <div class="card text-center">
                <div class="card-body">
                    
                        <h3 class="card-title text-uppercase">
                            {{name}}
                        </h3>
                    <p class="m-2">{{precio}} Bs</p>
                    <p class="m-2">{{description}}</p>
                    <p class="text-muted">
                        {{timeago created_at}}
                    </p>
                    <a class="btn btn-danger" href="/producto/delete/{{id}}">
                        Eliminar producto
                    </a>
                    <a class="btn btn-secondary" href="/producto/edit/{{id}}">
                        editar
                    </a>
                </div>
            </div>
        </div>
        {{else}}
        <div class="col-md-4 mx-auto">
            <div class="card card-body text-center">
                <p>Aún no hay productos guardados.</p>
                <a href="/producto/add">Crear nuevo</a>
            </div>
        </div>
        {{/each}}
    </div>
</div>
</div>
</body>
</html>